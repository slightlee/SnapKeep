# Composables 规范

目标：抽离业务逻辑与状态管理，避免视图层膨胀。

原则

1. 每个 composable 只负责一个领域（例如主题、备份、统计）。
2. 不直接依赖具体组件，保持可复用。
3. 不在 composable 内直接操作 DOM，必要时封装为小函数并保持边界清晰。
4. 对外暴露最小必要 API，避免泄露内部状态。

目录约定

1. 使用 `useXxx` 命名导出函数。
2. 统一通过 `index.js` 导出供视图层引用。
