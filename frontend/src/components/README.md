# Components 规范

目标：保持组件“可复用、低耦合、易测试”。

原则

1. 组件只负责展示与交互，不直接访问数据库或持久化。
2. 组件通过 `props` 输入数据，通过 `emit` 输出事件。
3. 单文件组件命名使用 PascalCase，例如 `InputSection.vue`。
4. 复杂组件优先拆分为更小的职责单元。
5. 避免在组件内定义跨模块常量，统一放在 `src/constants/`。

目录约定

1. `index.js` 统一导出组件，视图层只从该入口导入。
2. 组件样式放在全局样式表中，不在组件内内联大段样式。
